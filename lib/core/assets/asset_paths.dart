import 'animation_paths.dart';
import 'icon_paths.dart';
import 'image_paths.dart';

/// Asset paths constants - aggregates all asset path classes
class AssetPaths {
  // Base paths
  static const String images = ImagePaths.base;
  static const String icons = IconPaths.base;
  static const String animations = AnimationPaths.base;

  // Prevent instantiation
  AssetPaths._();
}

