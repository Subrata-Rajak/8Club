# 8Club Environment Variables
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control

# API Configuration
# Base URL for API calls (defaults to staging if not set)
BASE_URL=<YOUR BASE URL>

# API Timeout Configuration
# Timeout in seconds for API requests (defaults to 30 if not set)
API_TIMEOUT_SECONDS=<YOUR TIMEOUT IN SECONDS>
